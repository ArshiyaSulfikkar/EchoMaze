# Optional audio files

Place these files in this folder to enable the Phaser sound cues:

- `step.wav` — plays after a successful tile move
- `success.wav` — plays once when the player reaches the exit

The game continues normally if either file is absent. Future Web Audio API spatial-audio work belongs in `GameScene.js`.
